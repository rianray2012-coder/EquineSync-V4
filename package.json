{
  "name": "equinesync-v4-deploy",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci --legacy-peer-deps && npm --prefix frontend run build",
    "deploy": "npm run build && wrangler deploy",
    "deploy:cloudflare": "npm run deploy"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
