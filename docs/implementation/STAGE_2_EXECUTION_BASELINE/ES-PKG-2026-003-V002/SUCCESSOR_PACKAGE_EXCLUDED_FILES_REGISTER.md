# Successor Package Excluded Files Register

Excluded: `.git/`, editor files, caches, bytecode, temporary extraction trees, local absolute paths, secrets, live data, unrelated workspace artifacts, and mutable duplicates of the sealed predecessor. The predecessor is referenced by identifier and SHA-256 only.
