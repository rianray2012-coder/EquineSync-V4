# C0-015 Normalized Text Comparison

Expected source recovered: `False`. Raw-byte identical: `False`. Normalized nonblank-line similarity: `not available`. Heading similarity: `not available`. Normalization was used only for comparison and never as source-identity evidence.
