# C0-028 Normalized Text Comparison

Expected source recovered: `True`. Raw-byte identical: `False`. Normalized nonblank-line similarity: `1.0`. Heading similarity: `1.0`. Normalization was used only for comparison and never as source-identity evidence.
