# Phase 4 — Multi-tenancy & Permissions Map

> Status: **4A complete (foundations); Phase 4B DONE (4B-1…4B-7); 4C Permissions Wiring + 4D Registration/Invite Barn-Binding complete; 4E cross-tenant isolation suite COMPLETE (no leaks).** Phase 4 is now fully closed. Each sub-phase is commit-worthy, additive, and rollback-safe, gated on explicit approval.

## Approved design decisions (locked)
1. **`barn_id` is canonical.** The task engine's existing `tenant_id="default"` is mapped to `barn_id="primary"` **at the boundary** — no global rename. (Reconciliation of task-engine docs/router is a dedicated 4B sub-phase.)
2. **One user → one barn** for v1. Multi-barn membership/switching deferred.
3. **All existing + new users map to the canonical `primary` barn** in 4A/4B; true multi-barn signup delivered in **4D** (invite-only / founder-provisioned). **Public registration stays low-privilege (`horse_owner`) — never creates admins.**
4. **Lightweight centralized capability map** (`resource:action` → roles) + `require()` helper. No tightening in 4A.
5. **Canonical `barn_id = "primary"`** for the primary barn. Startup migration is idempotent + additive.

---

## 4A — Foundations (done 2026-06-04)

**New modules**
- `core/tenancy.py` — `PRIMARY_BARN_ID="primary"`, `resolve_barn_id(user)` (missing/empty/None ⇒ primary, legacy-safe), `barn_filter(user, extra)`, `stamp_barn(user, doc)`. Pure; no DB; no `server.py` import.
- `core/permissions.py` — `CAPABILITIES` map (`"barn:manage" → {admin, barn_manager}`), `has_capability`, `require` (fail-closed on unknown capability), and `require_setup_role` re-expressed through `require(...,"barn:manage")` (behavior-identical, same 403 message). **Defined + tested only — NOT wired into routes in 4A.**

**JWT / user `barn_id` handling**
- Both auth implementations updated in lockstep: `core/auth.py` (app-wide dependency) **and** `routes/auth.py` (its private `create_token` + `make_current_user_dependency`).
- `create_token(user_id, role, barn_id=None)` adds an **optional, forward-compat** `barn_id` JWT claim.
- `get_current_user` (both paths) attaches `user["barn_id"] = resolve_barn_id(user)` from the **fresh user document** — the document is the **source of truth** for authorization/scoping; the JWT claim is never trusted for isolation.
- `login` / `register` / `refresh` / invite-accept now pass the resolved `barn_id` into `create_token`.

**Creation paths stamp `barn_id`**
- `routes/auth.py::register` → new user gets `barn_id="primary"` (role stays `horse_owner`).
- `routes/invites.py::create_invite_with_link` → **ignores any client-supplied `body.barn_id`** and binds the invite to `resolve_barn_id(current_user)` (a barn-2 admin can only invite into barn 2). The accept flow binds the `new_user` (+ `onboarding_progress`) to the **invite's validated `barn_id`** since **4D**, with a legacy-safe fallback to `primary` if that barn is missing/deleted.
- `routes/onboarding.py` → stamps `barn_id=resolve_barn_id(user)` at creation time on `onboarding_progress` (both `_ensure_progress` and the `POST /onboarding/reset` upsert), `locations`, `feed_templates`, `inventory`, `recurring_schedules`, and CSV-imported `horses` + `owners` (no longer relying solely on the startup backfill). Staff invites stamp `barn_id="primary"`. The `barn` singleton is intentionally left keyed by its `id` (unchanged).
- `seed_data.py` → idempotent end-of-seed sweep stamps `barn_id="primary"` on all seeded collections.

**Idempotent startup migration** (`core/lifespan.py::_backfill_barn_id`, additive only, logged once)
- `update_many({"barn_id": {"$exists": False}}, {"$set": {"barn_id": "primary"}})` across these **25 domain collections**:
  `users, horses, owners, riders, medications, medication_logs, feed_tasks, vet_records, farrier_history, injuries, wellness, lessons, training, invoices, messages, service_requests, incidents, locations, feed_templates, inventory, recurring_schedules, staff_invites, invites, onboarding_progress, events`.
- First boot backfilled **3296 documents**; subsequent boots = 0 (idempotent — no log line).

**Explicitly NOT touched in 4A** (with reasons)
- **Task engine** (`tasks, task_templates, task_completions, task_events`) + **`media`** — these use `tenant_id="default"`; reconciled to `barn_id` in the dedicated **4B task-engine sub-phase** (alias: `tenant_id="default"` ≡ `barn_id="primary"`).
- **Notification collections** (`notifications, notification_preferences, notification_digest_log`) — user-keyed; isolation follows user→barn.
- **`barn` singleton** — keyed by its own `id="primary"` (that id *is* the barn id); adding a redundant field would change `GET /barn` response shape.
- **Auth/session/attempt infra** (`auth_tokens, refresh_tokens, login_attempts`) — user/token-scoped, no barn needed.

**Tests** (all green)
- `tests/test_tenancy.py` (13) + `tests/test_permissions.py` (7) — pure unit (incl. `barn_filter` override-rejection tests).
- `tests/test_core_auth_verification_gate.py` extended (+3): both auth paths attach `barn_id`.
- `tests/test_phase4a_barn_id.py` (live): public registration → `barn_id="primary"` + role forced `horse_owner`; invite create ignores client `barn_id="other"`; invite accept of a legacy `barn_id="other"` invite → user resolves to `barn_id="primary"` (the non-existent barn triggers 4D's legacy-safe fallback); `POST /onboarding/reset` before progress exists → created `onboarding_progress` is `primary`; onboarding creates (`locations`/`feed-templates`/`inventory`/`recurring-schedules`) and `csv-commit` horses+owners stamp `barn_id="primary"`.
- Full suite: **323 passed / 3 skipped** (a transient HTTPS connection flake to the preview host can occur under full-suite load; passes on clean re-run — unrelated to logic).

**Guardrails honored:** no read/write scoping yet, no route behavior changes, additive-only migration, no `server.py` imports from core, Security Patch 2E + both email-verification gates preserved.

---

## Next sub-phases (NOT started — await approval)
- **4B-1 ✅ — Horses** (`routes/horses.py`, done 2026-06-04). Scoped `GET /horses` list via `barn_filter(user)`; `GET/PATCH /horses/{id}` filter on `id`+`barn_id` (cross-barn ⇒ **404**, no existence leak); `POST /horses` stamps `barn_id` via `stamp_barn(user, doc)`; free-form `PATCH` strips `barn_id`/`id` so a horse can never be moved between barns. New `tests/test_horses_scoping.py` (4): other-barn exclusion from list, GET+PATCH 404, POST→primary, PATCH-cannot-move. Full suite **327 passed / 3 skipped**. *(Scope from the fresh user doc via `resolve_barn_id` only; no task_engine/media changes.)*
- **4B-2 ✅ — Care** (`routes/care.py`, done 2026-06-04). Scoped reads + stamped writes on the **7 engine-independent** collections: `owners`, `riders`, `medications`, `medication_logs`, `feed_tasks`, `injuries`, `wellness` (lists via `barn_filter(user, extra)`; creates via `stamp_barn`). `POST /feed-tasks/{id}/complete` now filters `id`+`barn_id` → cross-barn **404** with no mutation. `POST /wellness` stamps `barn_id` **and** scopes the cross-domain `horses.wellness_score` bump via `barn_filter(user, {"id": horse_id})` so a cross-barn `horse_id` triggers no side effect (no new horse-existence validation added — scoping only). `POST /vet-records` stamps `barn_id`. **Deferred to 4B-7:** `GET /vet-records` and `GET /farrier-history` reads are intentionally **NOT** barn-scoped yet, because `task_engine.py` completion hooks (lines 514/524) write those rows **without** `barn_id`; scoping their reads now would hide engine-projected rows and break engine completion tests. Read-scoping for both is bundled with the engine reconciliation in 4B-7. New `tests/test_care_scoping.py` (16): per-collection other-barn exclusion + POST→primary for the 7 (+vet POST stamp), feed-complete 404+no-mutation, wellness cross-barn-horse guard. Full suite **343 passed / 3 skipped**. *(Strict `barn_filter` unchanged; no task_engine/media changes.)*
- **4B-3 ✅ — Operations** (`routes/operations.py`, done 2026-06-04). Scoped reads + stamped writes on all 5 collections: `lessons`, `training`, `messages`, `service_requests`, `incidents` (lists via `barn_filter(user, extra)`; creates via `stamp_barn`). `POST /service-requests/{id}/approve` and `/decline` keep the role-403 gate first, then a **barn-scoped** lookup → cross-barn **404** (no mutation), preserving the 409-pending guard. All cross-domain name-enrichment lookups (`riders`, `horses`, **`users`/trainer**) are `barn_filter`-scoped so a cross-barn id resolves to `None` (no name leak; tolerate-missing preserved, no new validation). Deferral check: only `seed_data.py` writes these (already stamps primary) — **no engine/digest co-writer, no deferral needed**. New `tests/test_operations_scoping.py` (15): per-collection exclusion + POST→primary, SR approve/decline cross-barn 404+no-mutation, and rider/horse/trainer cross-barn name-leak guards. Full suite **358 passed / 3 skipped**. *(Strict `barn_filter` unchanged; no task_engine/media/dashboard changes.)*
- **4B-4 ✅ — Billing** (`routes/billing.py`, done 2026-06-04). Scoped `GET /invoices` via `barn_filter(user)`; `POST /invoices` stamps `barn_id` via `stamp_barn`; `POST /invoices/{id}/pay` filters `id`+`barn_id` → cross-barn **404** with no mutation, preserving the idempotent "set status=paid" semantics (no new 409/validation). No PATCH/DELETE and no barn_id/id-mutation surface (pay takes no body; `InvoiceIn` has no barn_id/id field). Deferral check: only `seed_data.py` writes invoices (stamps primary) and only `routes/dashboard.py` reads them (deferred to 4B-6) — **no engine/digest co-writer, no deferral needed**. New `tests/test_billing_scoping.py` (4): other-barn exclusion, POST→primary, cross-barn pay 404+no-mutation, same-barn pay happy path (id echoed, status=paid, paid_at present, barn_id stays primary). Full suite **362 passed / 3 skipped**. *(Strict `barn_filter` unchanged; dashboard invoice read untouched; no task_engine/media changes.)*
- **4B-5 ✅ — Onboarding & Reports** (`routes/onboarding.py` + `routes/reports.py`, done 2026-06-05). **Onboarding:** scoped the 5 list reads (`locations`, `feed_templates`, `inventory`, `recurring_schedules`, `staff_invites`) via `barn_filter(user)`; the 5 deletes now scope `delete_one(barn_filter(user, {"id": id}))` → cross-barn delete is a **silent no-op** (keeps idempotent `{"ok": True}`, no existence leak); `GET/PUT /barn` key-switched from hardcoded `"primary"` to `resolve_barn_id(user)` (response shape preserved; PUT forces `doc["id"]` to the resolved barn — client cannot alter it); `staff_invites` create now stamps `resolve_barn_id(user)` (was hardcoded `PRIMARY_BARN_ID`); CSV `preview`+`commit` dedup checks (`horses.name`, `owners.email`) and the owner-name→`owner_id` map are now `barn_filter(user)`-scoped so cross-barn rows neither flag duplicates, block imports, nor get linked. **Reports:** `setup_health_payload` / `nudge_candidates` / `send_nudges` gained an optional `barn_id=None`; admin routes (`/reports/setup-health`, `/reports/nudge-candidates`, `/admin/send-nudges`) pass `resolve_barn_id(user)` → scoped (`onboarding_progress`, `invites`, per-candidate `users` lookup); the startup `_nudge_loop` scheduler keeps `barn_id=None` → **global system-wide nudging (correct, no `core/lifespan.py` change)**. In v1 (all-primary) every path is behavior-identical. Onboarding wizard-state routes stay user-keyed (no read-scoping; PATCH never touches `barn_id`). New `tests/test_onboarding_reports_scoping.py` (14): per-collection list exclusion, create→primary, cross-barn delete no-op, barn key-switch, CSV cross-barn dup/owner guards, reports nudge-candidate + setup-health other-barn exclusion, plus two hardening tests — the `staff_invites` duplicate check is barn-scoped (an other-barn pending invite for the same email does not block/leak), and the `send_nudges` `onboarding_progress` update mutation is barn-scoped when `barn_id` is provided (an other-barn `user_ids` target yields no candidate and leaves `last_nudged_at`/`nudges_sent` unset). Two existing reports seeds in `test_reports_and_nudges.py` updated to include `barn_id="primary"` (reflects backfill reality). Full suite **376 passed / 3 skipped**. *(No task_engine/media/dashboard/permissions/.env/frontend changes.)*
- **4B-6 ✅ — Aggregations & Lists** (`routes/dashboard.py`, `routes/invites.py`, `routes/analytics.py`, done 2026-06-05). **Conservative scope — barn-native surfaces only; all task-engine reads left for 4B-7.** *Dashboard:* `/dashboard/summary` + `/dashboard/barn-board` now scope the 6 barn-native reads (`horses` total/stall_rest/wellness-avg, `injuries`, `service_requests`, `incidents`, `lessons`, `invoices`) via `barn_filter(user)`. The `tasks` (feed/med counts) + `task_completions` reads keyed by `tenant_id="default"` are **deliberately untouched** (→ 4B-7); in v1 `tenant_id="default" ≡ barn="primary"` so the dashboard stays internally consistent. *Invites:* `GET /invites` list → `barn_filter(user)`; the pending-invite duplicate check → `barn_filter(user, {"email":…, "status":"pending"})` (another barn's pending invite can't block/leak); `POST /{id}/resend` + `/{id}/revoke` scope the lookup → cross-barn **404** with no mutation (resend uses a single `scope = barn_filter(user, {"id": invite_id})` for its find/update/readback; revoke preserves its matched_count==0 → 404). The create path's send-history update + readback also use `barn_filter(user, {"id": invite["id"]})` for consistency. Unauthenticated token routes `GET /invites/verify` + `POST /invites/accept` are **unchanged** (no caller barn; new user already clamped to `primary` in 4A). *Analytics:* `POST /events` now stamps `barn_id=resolve_barn_id(user)`. **Deferred to 4B-7:** `GET /events/onboarding-funnel` barn-scoping — the internal `_track` (`core/analytics.py`) writes events from background loops with no barn context, so scoping the funnel now would drop server-emitted `onboarding.*` events; `core/analytics.py` was **not** modified. **Digests** (`routes/digests.py`/`owner_digest.py`) were **not touched** — they're owner-keyed (`horses.owner_id`, inherently barn-isolated in v1) and their substantive reads are `task_events`/`tasks` (→ 4B-7). New `tests/test_dashboard_invites_scoping.py` (8): dashboard summary other-barn count exclusion, barn-board list exclusion, `GET /invites` exclusion, pending-dup no-block, same-barn resend happy path, resend/revoke cross-barn 404+no-mutation, `POST /events`→primary. Full suite **384 passed / 3 skipped**. *(No task_engine/media/core.analytics/digests/care/operations/billing/onboarding/reports/permissions/.env/frontend changes.)*
- **4B-7 ✅ — Task-Engine & Media Reconciliation** (`task_engine.py`, `storage.py`, `routes/dashboard.py`, `routes/analytics.py`, `routes/care.py`, `owner_digest.py`, `core/lifespan.py`, done 2026-06-05). **Strategy A (additive dual-key): `barn_id` added everywhere; `tenant_id="default"` kept as the engine partition — never rewritten.** *Migration:* `core/lifespan.py` `BARN_BACKFILL_COLLECTIONS` extended with `tasks, task_templates, task_completions, task_events, media` → idempotent boot backfill stamped `barn_id="primary"` on ~7,487 legacy engine/media docs (0 missing post-run). *Engine write-stamping:* `barn_id` flows down the chain — `create_template`/`seed_starter_templates` stamp from the actor (or `PRIMARY_BARN_ID`), materialized `tasks` inherit from the **template**, `task_completions`/`task_events`/follow-ups inherit from the **task**, ad-hoc `POST /tasks` stamps from the user; the background `_materialize_loop` stays global (`materialize_all(barn_id=None)`) and needs **no user**. *Route scoping (all `tenant_id` + `barn_id=resolve_barn_id(user)`):* `GET /task-templates`, `/tasks`, `/tasks/today` (both windows), `/horses/{id}/timeline`, `/staff/{id}/activity`, `/tasks/analytics/summary` (all 3 queries). *By-id mutations → cross-barn 404/no-mutation:* `PATCH`/`DELETE /task-templates/{id}` (find-guard + scoped update/delete/cancel), `PATCH`/`reassign /tasks/{id}`, and engine `complete_task`/`skip_task`/`void_completion` (resolve barn from `user`, add to by-id finds). `POST /tasks/materialize` scopes to the caller's barn templates. *Deferred items closed:* `_build_vet_record`/`_build_farrier_record` now stamp `barn_id` → `care.py` `GET /vet-records` + `/farrier-history` switched to `barn_filter(user)` (4B-2 deferral resolved); `dashboard.py` feed/med/`meds_missed` + barn-board feed/med add `barn_id`; `analytics.py` onboarding-funnel `$match` adds `barn_id` (clean — no `core/analytics.py` change, no `_track` onboarding writers exist); `owner_digest.py` derives `owner_barn` from the already-fetched owner horses and adds `barn_id` to all `task_events`/`tasks` reads (defense-in-depth atop owner-keying). *Media:* dormant (0 docs, no live routes) — `storage.py` `MediaRecord` + `build_key` gain a `barn_id` namespace (forward-compat only). *Indexes:* additive index changes **skipped** (zero selectivity gain in single-barn v1; documented follow-up). **Hardening (Codex round):** (1) the additive `barn_id` backfill now runs **before** `seed_starter_templates`/`materialize_all` at startup, so the barn-scoped occurrence-dedup sees legacy (barn_id-less) tasks and never creates duplicate occurrences on a legacy DB boot; (2) `complete_task`/`void_completion` carry `tenant_id`+`barn_id` through **every** `task_completions`/`tasks` find & update (idempotency, canonical, status update, duplicate-note, void update/readback) — a colliding `task_id`/completion id in another barn can no longer be returned or mutated; (3) `owner_digest.py` horse projections now include `"barn_id": 1` so `owner_barn` derives correctly (was always falling back to `"primary"`). New `tests/test_taskengine_reconciliation_scoping.py` (13): migration completeness, write-stamping, by-id cross-barn 404+no-mutation, list+timeline exclusion, vet/farrier read scoping, dashboard task counts, onboarding-funnel exclusion, **backfill-before-materialize no-duplicate**, **cross-barn canonical-collision on complete**, **cross-barn collision on void**. Full suite **397 passed / 3 skipped**. *(No `tenant_id` rewrite; no `core/analytics.py`/permissions/multi-barn signup/frontend/.env changes.)*
- **4C ✅ — Centralized Permission Enforcement** (`core/permissions.py`, `core/auth.py`, `routes/auth.py`, `routes/operations.py`, `routes/digests.py`, `routes/admin.py`, done 2026-06-05). **Behavior-identical wiring** — same 403 status, same exact messages, same role sets. *Capability map extended:* `service_request:approve` + `service_request:decline` {admin, barn_manager, trainer}, `digest:read_own` {horse_owner}, `digest:admin` {admin, barn_manager}, `admin:access` {admin} — each with its verbatim legacy denial message (unknown capabilities still fail closed). *Swaps:* `core/auth.py::require_setup_role` now delegates to `require(user, "barn:manage")` (propagates to all existing callers — reports/invites/analytics/onboarding/barn settings — with zero behavior change); the **dead duplicate** `routes/auth.py::require_setup_role` (zero callers) deleted; `operations.py` SR approve/decline, `digests.py` 4 owner/admin gates, and `admin.py` `tenant_reset` swapped to `require(...)`. *Left intact (not RBAC-`require` shape):* `core/config.py::evaluate_seed_access` tuple logic, email-verification 403s, invite target-role validation, and the `has_setup_role` derived flag. New tests: `test_permissions.py` extended (per-capability allow/deny + exact messages + fail-closed) and new `test_permissions_wiring.py` (9 route-level — denied roles get the exact 403 message, allowed roles pass the gate). Full suite **411 passed / 3 skipped**. *(No status/message/behavior changes; no `.env`/frontend/multi-barn work.)*
- **4D ✅ — Registration & Invite Barn-Binding** (Option A: invite-only / founder-provisioned) (`routes/barns.py` *new*, `server.py`, `routes/invites.py`, `routes/auth.py`, `core/permissions.py`, done 2026-06-05). **Invite accept** now binds the new user + their `onboarding_progress` to the **invite's validated `barn_id`** (was the 4A hardcoded `primary` clamp) — **legacy-safe**: a missing/deleted invite barn falls back to `primary` so a valid invitee is never locked out; existing primary invites still bind to `primary` (zero change). **New `POST /barns`** provisions a barn (`db.barn` doc, fresh `new_id()`) **plus** its first admin (role `admin`, `email_verified=True`, bound to the new barn) and an `onboarding_progress` row — gated by the new `barn:create` capability `{admin}` **and** a founder-barn guard (`resolve_barn_id(user) == "primary"`, message "Only the founder barn can create new barns") so a freshly-provisioned barn admin can't spawn further barns until a real `superadmin` role exists (backlog). **Public `/auth/register` unchanged** — stays bound to `primary` with role `horse_owner` (Security Patch 2E). **Invite create** unchanged (binds to inviter's barn). Invariants: one barn per user; **no switching/multi-membership** (deferred); authz still reads the fresh user doc. New `tests/test_barn_binding.py` (7): create-barn denied for non-admins (403 "Admin access required to create barns") and non-primary admins (403 founder-gate), founder creates barn+first-admin (admin can log in), second-barn invite accept binds user+progress to barn 2, barn-2 admin invites only into barn 2 (and primary can't see them), missing-barn invite → primary fallback, public register stays primary+horse_owner. Full suite **418 passed / 3 skipped**. *(No frontend/.env changes; no barn-switching.)*
- **4E** — two-barn cross-tenant isolation test suite (the security gate). ✅ **DONE 2026-06-06 — no leaks found.** Test-only phase: zero production code changed. Built a module-scoped two-barn world (`tests/_isolation_world.py`) that provisions a real Barn B via `POST /barns` (founder-gated), invites + accepts a Barn B `horse_owner`, and seeds parallel domain entities in BOTH barns through the live API (engine task completed in each). Strict teardown sweeps every Barn B doc by `barn_id` (+ barn/user/invite rows) and deletes each tracked Barn A test id, so suite counts are not polluted. **`tests/test_4e_isolation_core.py` (19)** — both-directions isolation across horses (list/get/patch 404 + cannot-move-barn), care (owners/medications/vet/injuries/wellness), operations (lessons/training/messages/SR/incidents + SR approve&decline cross-barn 404 no-mutation), billing (list + pay cross-barn 404 no-mutation), onboarding (5 lists + cross-barn delete silent no-op + `/barn` singleton key), dashboard (summary counts == per-barn DB counts; barn-board lesson lists scoped), invites (list excludes other barn; resend/revoke cross-barn 404), reports (setup-health `total_setups`/`invites.total` == per-barn DB counts), and the founder-gate (provisioned Barn B admin gets 403 on `POST /barns`). **`tests/test_4e_isolation_engine.py` (8)** — task-templates (list both-directions; patch/delete cross-barn 404 no-mutation), tasks (today-windowed list both-directions; patch/reassign/skip/void cross-barn 404 no-mutation), horse-timeline + staff-activity cross-barn return empty (no event leak), analytics-summary `completions` == per-barn DB count (B disjoint < A), and owner-digest never surfaces another barn's horse name. Full suite **445 passed / 3 skipped** (418 baseline + 27 new). *(No production/route/.env/frontend changes — pure additive tests + this doc.)*
