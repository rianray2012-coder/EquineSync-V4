# CODEX DIRECTIVE

## EquineSync Item 08 LTRG PIA V0.2.1 Canonical Remediation and Compliant Reexecution

**Directive status:** Founder-authorized documentary remediation and repository integration attempt  
**Authority effect:** Documentary only  
**Repository:** `rianray2012-coder/EquineSync-V4`  
**PIA program position:** `08`  
**Canonical PIA ID:** `ES-PIA-LESSONS-TRAINING-RIDER-GUARDIAN`

---

## 1. Founder Authorization

The Founder authorizes Codex to:

1. preserve the failed Item 07 intake and its receipt as immutable historical evidence;
2. remediate the Lessons, Training, Rider, and Guardian PIA package so that it uses the repository-native Item 08 sequence and canonical PIA identifier;
3. create a new V0.2.1 canonical-remediation candidate package with regenerated manifests, validation records, checksum ledgers, and archive hash;
4. perform a fresh repository-native structured review only in a runtime that satisfies the repository's formal review gate;
5. integrate the documentary Item 08 package into the official repository on a new bounded branch if every preflight, runtime, identity, integrity, scope, and validation gate passes;
6. commit and push the resulting documentary branch and verify exact local/remote commit agreement.

This directive does **not** authorize:

- application-code changes;
- schemas or migrations;
- implementation;
- deployment;
- production use;
- pilot activity;
- first-user enrollment;
- a pull request;
- a merge;
- modification of locked governance sources;
- retroactive alteration or deletion of the failed Item 07 evidence.

Proceed fail-closed.

---

## 2. Inputs

Use the following materials as source evidence:

1. `EquineSync_Item_07_LTRG_PIA_V0_2_Codex_Handoff.zip`
2. `EquineSync_Item_07_LTRG_PIA_V0_2_Codex_Handoff.zip.sha256`
3. `ITEM_07_LTRG_PIA_FAIL_CLOSED_RECEIPT.md`
4. the official repository and its repository-native PIA program records.

The failed Item 07 package is a historical source package. It is not the active repository identity for LTRG.

Do not overwrite, silently rename, delete, or rewrite the historical Item 07 archive or receipt.

---

## 3. Mandatory Preflight Sequence

Complete every step below before any repository mutation, branch creation, commit, or push.

### 3.1 Establish an isolated workspace

- Use a fresh isolated working directory.
- Do not reuse the prior failed clone or extracted intake directory as the mutable integration workspace.
- Preserve source package bytes separately and read-only where practical.
- Record all absolute paths used in the final receipt.

### 3.2 Verify package integrity

For the historical Item 07 source package:

- verify the outer ZIP SHA-256 against the supplied sidecar;
- run `unzip -t`;
- verify every supplied checksum ledger;
- verify manifest filename, size, and hash agreement;
- verify the embedded directive and any standalone directive according to the package's own manifest;
- record the historical candidate Markdown and DOCX hashes;
- confirm that the supplied validation reports `overall: PASS` and `authority_effect: NONE`.

Any mismatch is an immediate stop condition.

### 3.3 Verify repository identity and remote

Confirm:

- repository name: `rianray2012-coder/EquineSync-V4`;
- official remote URL;
- remote HEAD;
- current remote refs;
- current latest relevant PIA-line branch and commit;
- clean fresh-clone state before analysis.

Do not assume the values recorded in the prior receipt remain current. Fetch and verify them.

### 3.4 Verify the repository-native PIA sequence

Read the repository-native active program records, including at minimum:

- `REMAINING_PIA_MASTER_REGISTER.csv`;
- any controlling PIA program manifest or sequencing register;
- any current Item 07 and Item 08 records;
- the repository's naming and destination conventions from the latest successfully integrated PIA items.

Required conclusion:

- Position `07` is `ES-PIA-CARE-OPERATIONS`.
- Position `08` is `ES-PIA-LESSONS-TRAINING-RIDER-GUARDIAN`.

If the repository records no longer support that conclusion, or if multiple active records conflict, stop and issue a new fail-closed receipt. Do not guess.

---

## 4. Runtime Permission Gate

The runtime gate must be evaluated before any formal reviewer role starts and before any repository mutation.

Read and apply the repository-native `PROGRAM_REVIEW_RUNTIME_GATE.md` and any successor or controlling runtime policy.

### 4.1 Formal reviewer posture

A formal repository-native structured reviewer must run with:

- read-only access;
- approval policy set to on-request;
- network disabled.

### 4.2 Writable validation and integration posture

Any bounded writable validation or documentary repository integration step must use:

- an isolated workspace-write scope;
- approval policy set to on-request;
- network disabled, except for a separately controlled fetch/push step if the repository policy expressly permits it;
- no unrestricted filesystem posture.

### 4.3 Stop rule

If the active Codex runtime is unrestricted, uses `approval_policy=never`, has network enabled contrary to the controlling gate, or otherwise fails the repository's required posture:

- start zero formal reviewer roles;
- create no branch;
- mutate no repository files;
- make no commit;
- perform no push;
- claim no independent or formal structured review;
- preserve all source evidence;
- issue a fail-closed receipt identifying the exact runtime mismatch.

Do not attempt to cure the runtime gate by merely documenting the mismatch after work has started.

---

## 5. Canonical Remediation Requirements

Create a new documentary successor identified as:

- **Program item:** `08`
- **Canonical PIA ID:** `ES-PIA-LESSONS-TRAINING-RIDER-GUARDIAN`
- **Candidate version:** `V0.2.1`
- **Candidate status:** Canonical Remediation Candidate
- **Authority effect:** `NONE`

### 5.1 Required identity corrections

Correct all active-candidate references from Item 07 to Item 08, including:

- document titles;
- title pages;
- headers and footers;
- portfolio position;
- PIA identifier;
- filenames;
- internal cross-references;
- package README;
- Founder approval record;
- review and revision report;
- validation record;
- package manifest;
- checksum ledgers;
- archive name;
- archive sidecar hash;
- Codex directive;
- intended repository destination;
- any active program-index entry created during integration.

Use the singular canonical domain terms:

- `RIDER`
- `GUARDIAN`

Do not use the noncanonical active identifier:

`ES-PIA-LESSONS-TRAINING-RIDERS-GUARDIANS-V0.2.0`

### 5.2 Substantive-preservation rule

The identity remediation must not silently change the Founder-approved product decisions or substantive requirements.

Preserve:

- `LTRG-FD-001` through `LTRG-FD-020`;
- all substantive V0.2 requirements unless a fresh structured review produces an evidence-backed documentary correction;
- the five-question answer posture;
- the documentary-only authority boundary.

Any substantive edit arising from fresh review must be:

- explicitly identified;
- reasoned;
- traceable to a finding;
- included in the review report;
- reflected in regenerated validation and checksums.

### 5.3 Historical evidence preservation

Preserve the original Item 07 package and fail-closed receipt as historical evidence outside the active Item 08 candidate family.

Do not present the Item 07 candidate as though it had been successfully integrated.

---

## 6. Required New Package

Create a new package using a clear canonical name, preferably:

`EquineSync_Item_08_LTRG_PIA_V0_2_1_Canonical_Remediation_Codex_Handoff.zip`

The package should contain, at minimum:

1. Item 08 V0.2.1 candidate Markdown;
2. Item 08 V0.2.1 candidate DOCX;
3. V0.1 historical predecessor Markdown and DOCX, clearly marked historical;
4. V0.2 internal-review predecessor materials, clearly marked superseded where applicable;
5. V0.2.1 remediation and structured-review report;
6. Founder approval and remediation authorization record;
7. deterministic documentary validation record;
8. package README;
9. package manifest;
10. internal checksum ledger;
11. `CODEX_DIRECTIVE.md`;
12. any repository-native review outputs required by the master program.

Regenerate every derivative artifact after the canonical corrections. Do not reuse stale manifests, stale validation JSON, stale checksums, or the prior ZIP sidecar.

---

## 7. Fresh Structured Review

A fresh repository-native structured review is required before integration, but only if the runtime gate passes.

### 7.1 Review basis

Review the Item 08 V0.2.1 candidate against:

- the controlling Master PIA Standard and template;
- the repository-native PIA program requirements;
- approved Founder decisions `LTRG-FD-001` through `LTRG-FD-020`;
- applicable locked governance sources;
- MIAP traceability requirements;
- naming, sequencing, evidence, validation, and repository conventions established by prior successfully integrated PIAs.

### 7.2 Review claims

Do not call a review independent unless the repository's independence requirements are actually satisfied.

Clearly label:

- internal drafting review;
- deterministic validation;
- formal structured review;
- independent review, only if genuinely qualified.

### 7.3 Five mandatory questions

The review must address all five mandatory questions using the controlling exact wording and allowed answer vocabulary.

The expected documentary posture is:

1. Engineering buildability: `YES_WITH_EVIDENCE`
2. Objective QA verification: `YES_WITH_EVIDENCE`
3. Governance and MIAP traceability: `YES_WITH_EVIDENCE`
4. Operational safety and recovery: `NO`
5. First-user enrollment readiness: `NO`

Do not force the expected answer if the actual evidence does not support it.

Questions 4 and 5 must remain negative absent approved as-built evidence, executed verification, staffed operations, tested recovery, production monitoring, rollback evidence, support readiness, and controlled enrollment evidence.

---

## 8. Baseline and Destination Selection

After runtime, identity, and review gates pass:

1. identify the latest authoritative PIA integration baseline;
2. verify that the baseline includes all prior required PIA items, including the current canonical state of Item 07 Care Operations;
3. select the repository destination by applying repository-native conventions from prior PIA items;
4. record the exact starting branch and commit;
5. stop if destination or baseline remains ambiguous.

Do not select the default remote HEAD merely because it is the default branch if the active PIA integration line is elsewhere.

---

## 9. Branch and Scope

Use a new, descriptive branch, for example:

`codex/item-08-ltrg-pia-canonical-remediation-fresh-review-v1`

The branch must contain documentary and governance-program integration changes only.

Permitted changes:

- the new Item 08 LTRG PIA package;
- bounded PIA program indexes, manifests, registers, and status records needed to register Item 08;
- review, validation, evidence, and receipt files;
- documentary corrections directly required by the canonical remediation.

Prohibited changes:

- application code;
- runtime code;
- database schemas;
- migrations;
- deployment files;
- unrelated formatting;
- unrelated repository cleanup;
- changes to locked governance source bytes;
- modifications to historical evidence except additive custody references.

If any prohibited change appears in the worktree or staged diff, stop and resolve it before continuing.

---

## 10. Validation Gates

Before commit, validate at minimum:

### 10.1 Package validation

- outer ZIP hash;
- `unzip -t`;
- all checksum ledgers;
- manifest parity;
- filename and metadata agreement;
- Markdown/DOCX pair identity;
- validation JSON status;
- no stale Item 07 active-candidate references;
- exact canonical PIA ID;
- exact Item 08 sequence.

### 10.2 Documentary validation

- all mandatory master-template sections are present and ordered;
- all five questions are fully answered;
- Founder decisions `LTRG-FD-001` through `LTRG-FD-020` are traceable;
- requirements, acceptance criteria, tests, golden paths, adversarial scenarios, risks, findings, dependencies, and evidence mappings pass repository validators;
- cross-PIA ownership boundaries remain explicit;
- authority boundary remains documentary only.

### 10.3 Repository validation

- exact intended files only;
- no locked-source byte changes;
- no application-code changes;
- no untracked package residue;
- no staged files outside the bounded integration scope;
- program registers agree on Item 08 identity and status;
- worktree and index state are recorded;
- any repository-required documentary tests pass.

### 10.4 Review gate

- formal review runtime posture passed before roles started;
- formal roles and outputs are recorded accurately;
- no unsupported independent-review claim;
- all blocking findings are closed or the process stops without integration.

---

## 11. Commit and Push

Only after every gate passes:

1. stage only the bounded documentary integration set;
2. capture the exact staged file list;
3. create one documentary integration commit, preferably:

   `docs(governance): remediate and review Item 08 LTRG PIA`

4. push the new branch to the official remote;
5. verify that the local commit and remote branch ref match exactly;
6. confirm the final worktree and index are clean.

Do not create a pull request.  
Do not merge.  
Do not change the repository default branch.  
Do not create or move a protected tag.

---

## 12. Required Final Receipt

Produce a final receipt containing:

1. disposition;
2. authority effect;
3. official repository and remote;
4. isolated workspace path;
5. source package paths and hashes;
6. historical Item 07 custody statement;
7. verified repository-native Item 08 identity;
8. runtime-gate result and exact posture;
9. starting baseline branch and commit;
10. resulting branch and commit, if created;
11. remote-ref verification;
12. canonical repository destination;
13. complete integrated file inventory;
14. package archive name and SHA-256;
15. validation and test results;
16. structured-review status and reviewer-role accounting;
17. all five mandatory question answers;
18. open findings, retained limitations, and deferred evidence;
19. confirmation of no application code, schema, migration, deployment, production, pilot, or enrollment authority;
20. confirmation that no PR or merge was created.

Use one of these dispositions, as supported by evidence:

- `ITEM_08_LTRG_PIA_CANONICAL_REMEDIATION_INTEGRATED_AND_VERIFIED`
- `ITEM_08_LTRG_PIA_CANONICAL_REMEDIATION_BLOCKED`
- `ITEM_08_LTRG_PIA_REVIEW_BLOCKED_BY_RUNTIME_GATE`
- `ITEM_08_LTRG_PIA_BLOCKED_BY_SEQUENCE_OR_BASELINE_AMBIGUITY`
- `ITEM_08_LTRG_PIA_BLOCKED_BY_VALIDATION_FAILURE`

Do not report success unless the remote ref, repository state, package integrity, and all mandatory gates are verified.

---

## 13. Fail-Closed Rule

At every stage, uncertainty is a stop condition.

Stop before mutation when:

- package integrity fails;
- repository identity is uncertain;
- Item 08 sequence is not authoritative;
- the canonical ID conflicts;
- runtime posture fails;
- baseline or destination is ambiguous;
- a formal review cannot lawfully or accurately be claimed;
- a blocking finding remains open;
- validation fails;
- the proposed diff exceeds documentary scope;
- remote push target is uncertain.

Preserve evidence, state exactly what passed and failed, and issue the required receipt.

---

## 14. Final Authority Boundary

This directive authorizes a documentary canonical-remediation and repository-integration attempt only.

It does not authorize implementation, application behavior, schemas, migrations, deployment, production use, pilot enrollment, first-user enrollment, or any representation that the operational system has been built, verified, or approved for use.
