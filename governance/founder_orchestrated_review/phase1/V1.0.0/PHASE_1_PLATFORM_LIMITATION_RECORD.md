# Phase 1 Platform Limitation Record

## Preserved controlling result

At predecessor `75c56ac67b0de694436c093fc2dc5ff5dffe4ff3`, static role configurations are present 8/8, but the supported runtime surfaces did not expose an exact custom-agent selector. The controlling machine-readable disposition is `FOUNDER_REVIEW_AGENTS_BLOCKED_BY_CONFIRMED_RUNTIME_PRODUCT_LIMITATION`. Earlier evidence preserves `agent_type=null`, `agent_role=null`, generic fallback, parent substitution, and the stopped first canary.

## Phase 1 treatment

This limitation does not revoke the approved framework. It prevents claims that a runtime-native canonical ES-RA role executed when the runtime cannot prove it. Phase 1 may use configuration-identified, isolated, manually bounded executions only when permission, context, input, output, and evidence controls pass; it may not relabel a generic runtime child as an approved role or as a distinct Reviewer Identity.

The current execution surface exposes a broader-than-authorized parent permission profile for formal ES-RA delegation. Without an express exception and host-enforced role boundary, the permission check fails closed. This blocks formal Pilot A role execution but not documentary preparation or deterministic tests confined to the authorized Phase 1 workspace.

Prior failed canaries and runtime results remain immutable historical evidence. Phase 1 does not retroactively cure them.
