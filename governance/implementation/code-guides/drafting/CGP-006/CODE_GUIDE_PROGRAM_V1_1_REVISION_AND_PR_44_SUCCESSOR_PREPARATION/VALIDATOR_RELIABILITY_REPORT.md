# Validator Reliability Report

New validators identify versions, inputs, evaluated rules, results, limitations, and owner. They fail nonzero on mandatory validation failure, report missing and malformed files, include positive, negative, malformed-input, and boundary fixtures, and remain shadow validation only.

No validator is recorded as an active merge gate.
