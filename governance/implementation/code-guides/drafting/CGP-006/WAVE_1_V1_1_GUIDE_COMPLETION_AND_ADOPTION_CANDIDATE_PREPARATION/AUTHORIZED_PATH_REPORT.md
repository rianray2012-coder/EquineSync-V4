# Authorized Path Report

Authorized write root:

```text
governance/implementation/code-guides/drafting/CGP-006/WAVE_1_V1_1_GUIDE_COMPLETION_AND_ADOPTION_CANDIDATE_PREPARATION/
```

All generated package artifacts, validators, tests, and fixtures are contained under this root. The directive prohibits changes to `PROGRAM_STATUS.md`, canonical registers, canonical adoption or activation registers, custody receipts, global schemas, global templates, global profiles, global validators, historical packages, frozen source records, adopted guide bytes, and application code.

Validation enforces that every changed path is inside the authorized package root.
