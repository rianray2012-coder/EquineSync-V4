# ES-CG-10 Package Versus Child Treatment Report

- Package or directory-level normative rows: `3`
- Child-file hashes remain preserved in the applicable manifest where a directory row is used.
- No normative row states that a package-level source could replace the individual row.
- `CGSRC-TEST-0001` uses package-level treatment for `backend/tests`.
- `CGSRC-TEST-0002` uses package-level treatment for `tests`.
- `CGSRC-CGP005-0207` uses package-level treatment for `backend/tests`.
