{
  "installCommand": "npm ci --legacy-peer-deps --include=dev --no-audit --no-fund",
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
